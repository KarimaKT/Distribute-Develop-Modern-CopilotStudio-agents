# Private session context (contains env URLs and bot IDs — keep local only)
CONTEXT.md

# Temp workspaces and build artifacts
_workspace_*/
_clone_*/
_inspect_*/
_push_*/
_skill_build/
_test_*/
_pb_*

# Export output (agent YAML, configs, deployable bundles — not for source control)
sample/
skills-with-assets/
*-bundle.zip
*-skill.zip
agent.zip
manifest.json

# PAC workspace state (env-specific, not for source control)
**/.mcs/

# OS
.DS_Store
Thumbs.db
